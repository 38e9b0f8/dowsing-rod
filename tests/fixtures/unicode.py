# Unicode identifiers and strings.

def grüße(名前):
    """日本語の挨拶を返す関数"""
    nachricht = f"こんにちは {名前}"
    return nachricht


def приветствие(имя):
    """Функция приветствия на русском"""
    сообщение = f"Привет {имя}"
    return сообщение
